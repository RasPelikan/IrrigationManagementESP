data
build
build_info.h
.DS_Store

# arduino-cli config (generated; toolchain itself lives in ~/Library/Arduino15-IrrigationManagementESP/)
.arduino-cli.yaml

# Toolchain snapshots (large; archive externally — see README "Cold-storage rebuild")
vendor

# Vite writes a transient config loader during build; would otherwise mark the
# tree dirty for the provenance check in vite.config.ts.
webapp/vite.config.ts.timestamp-*.mjs
